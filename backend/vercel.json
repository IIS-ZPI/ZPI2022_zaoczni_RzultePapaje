{
    "builds": [{
        "src": "zpiproject/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "zpiproject/wsgi.py"
        }
    ]
}